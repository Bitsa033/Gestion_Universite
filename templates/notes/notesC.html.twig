{% extends 'base.html.twig' %}
{% block body %}
    <div class="content-wrapper">
        <section class="content">
          <div class="card col-10 offset-1">
            <div class="card-header form-inline">Notes Collectives</div>
            <div class="card-body table-responsive p-0" style="height: 390px;" >
                <div class="row">
                    <div class="col-12">
                        <table class="table table-head-fixed text-nowrap table-bordered">
                            <tr>
                                <td>Semestre</td>
                                <td>Matiere</td>
                                {% for row in notes %}
                                    <td>{{row.etudiant}}</td>
                                {% endfor %}
                            </tr>
                            {% for row in notes2 %}
                            <tr>
                                <td>{{row.semestre}}</td>
                                <td>{{row.matiere}}</td>
                                {% for row in notes3 %}
                                    <td>{{row.moyenne}}</td>
                                {% endfor %}
                            </tr>
                            {% endfor %}
                            
                        </table>
                    </div>
                    <!--/col-->
                    {# <div class="col">
                        <table class="table table-head-fixed text-nowrap table-bordered">
                            <tr>
                                {% for row in notes %}
                                    <td>{{row.inscription.etudiant.nom}}</td>
                                {% endfor %}
                                {% for row in notes %}
                                    <td>{{row.moyenne}}</td>
                                {% endfor %}
                            </tr>
                        </table>
                    </div> #}
                </div>
                <!--/row-->
            
            </div>
            <div class="card-footer"><p class="h5">Pied de Page ...</p></div>
          </div>
          <!--/card-->
        </section>
    </div>
{% endblock %}
