{% extends 'base.html.twig' %}

{% block body %}
  <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">

    <!-- Main content -->
    <section class="content">
      <div class="container-fluid">
        <!-- Info boxes -->
        <div class="row">
          <div class="col-md-8">
              <a href="{{path('etudiants_ajout')}}"  class="btn btn-primary">Ajouter <i class="fa fa-plus"></i></a> 
              <a href="{{path('etudiants_i')}}"  class="btn btn-primary">Inscrire <i class="fa fa-pen"></i></a> 
    
                <div class="card ">
                    <div class="card-header">
                        <form action="">
                            <div class="form-inline">
                            <input type="text" class="form-control">
                            <button type="submit" class="btn btn-success">chercher</button>
                            </div>
                        </form>
                    </div>
                    <div class="card-body table-responsive p-0" style="height: 400px;">
                        <table class="table table-bordered table-head-fixed text-nowrap">
                            <tr>
                                <th style="width:1%;">ID</th>
                                <th style="width:1%;">Action</th>
                                <th >Etudiant</th>
                                <th>Inscrit</th>
                            </tr>
                            {% for etudiant in etudiants %}
                            <tr>
                                <td >{{etudiant.id}}</td>
                                {% if etudiant.filiere and etudiant.niveau  %}
                                <td>
                                {# <a href="{{path('etudiants_suppression',{id:etudiant.id})}}"  class="btn btn-warning"><i class="fa fa-times"></i></a>
                                <a href="{{path('etudiants_suppression',{id:etudiant.id})}}"  class="btn btn-info"><i class="fa fa-edit"></i></a> #}
                                </td>
                                <td>
                                  <a  href="">{{etudiant.nom}} </a>
                                </td>
                                <td><span class="btn btn-success">oui !</span></td>
                                {% else %}
                                <td>
                                <a href="{{path('etudiants_suppression',{id:etudiant.id})}}"  class="btn btn-warning"><i class="fa fa-times"></i></a>
                                <a href="{{path('etudiants_suppression',{id:etudiant.id})}}"  class="btn btn-info"><i class="fa fa-edit"></i></a>
                                </td>
                                <td>
                                   <a  href="">{{etudiant.nom}}</a>
                                </td>
                                 <td><span class="btn btn-danger">non !</span></td>
                                {% endif %}
                            </tr>
                            {% else %}
                            <tr>
                            <td colspan="5">Aucune donnée recente !</td>
                            </tr>
                            {% endfor %}
                        
                        </table>
                        
                    </div>
                </div>
          </div>
          <!-- col -->
        </div>
        <!-- row -->

    </section>
    <!-- /.content -->
    </div>
    <!-- /.content-wrapper -->

{% endblock %}

{% block javascripts %}
  <script src=""></script>
{% endblock %}


