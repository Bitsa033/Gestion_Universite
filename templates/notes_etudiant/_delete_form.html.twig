<form method="post" action="{{ path('notes_etudiant_delete', {'id': notes_etudiant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ notes_etudiant.id) }}">
<button class="btn btn-danger col-md-3">Supprimer la note</button>

</form>
