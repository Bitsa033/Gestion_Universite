
<h1>{{titre}}</h1>

<div>
    <table border='2' width='100%'>

        {% for classe in classes %}
            <tr>
                <td>{{classe.nom}}</td>
            </tr>
        {% else %}
            
        {% endfor %}

    </table>
</div>
